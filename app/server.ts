/**
 * @fileoverview 서버를 실행하는 루트 파일
 *
 * @todo vite의 서버처럼 파일 변경이 있으면 변경이 있는 파일만 다시 빌드하고 서버를 리로드하는 기능
 */

/**
 * 서버를 실행하는 함수
 */
const serve = () => {
  console.warn('Server is running on port 3000');
};

serve();
